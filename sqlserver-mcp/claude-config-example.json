{
  "_comment": "Configuración para Claude Desktop - SQL Server MCP Multi-Database",
  "_instructions": [
    "1. Ajusta la ruta en 'args' para que coincida con la ubicación de tu proyecto",
    "2. Configura tus bases de datos en databases.config.json", 
    "3. Agrega esta configuración a tu claude_desktop_config.json"
  ],
  "_note": "Con la nueva configuración JSON, ya no necesitas variables de entorno aquí!",

  "mcpServers": {
    "sqlserver": {
      "command": "node",
      "args": ["D:/mcp/sqlserver-mcp/index.js"]
    }
  }
}